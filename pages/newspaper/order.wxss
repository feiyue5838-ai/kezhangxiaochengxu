/* pages/newspaper/order.wxss */

/* ========== 閻庝絻澹堥崺鍛村冀?========== */

.nav-back {
  width: 60rpx;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 40rpx;
  color: #fff;
  font-weight: 300;
  line-height: 1;
}

.nav-title {
  font-size: 34rpx;
  font-weight: 600;
  color: #fff;
  letter-spacing: 1rpx;
}

.nav-placeholder { width: 60rpx; }

/* ========== Tab 闁?========== */
.tab-bar {
  top: 88rpx;  /* nav-bar height */
  position: fixed;
  left: 0;
  right: 0;
  z-index: 99;
  display: flex;
  background: #fff;
  padding: 0 32rpx;
  border-bottom: 1rpx solid #F8F7FC;
}

.tab-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 84rpx;
  position: relative;
}

.tab-text {
  font-size: 28rpx;
  color: #8C97A6;
  transition: color 0.2s;
}

.tab-item.active .tab-text {
  color: #7B5CFA;
  font-weight: 600;
}

.tab-line {
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 48rpx;
  height: 5rpx;
  background: #7B5CFA;
  border-radius: 20rpx;
}

/* ========== 閻犱降鍨瑰畷鐔煎礆濡ゅ嫨鈧?========== */
.order-list {
  padding-top: 188rpx;
  padding: 24rpx;
}

.order-item {
  background: #fff;
  border-radius: 20rpx;
  padding: 32rpx;
  margin-bottom: 20rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
  box-shadow: 0 2rpx 12rpx rgb(0 0 0 / 0.04);
}

.order-info { flex: 1; }

.order-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #1A2332;
  display: block;
}

.order-desc {
  font-size: 24rpx;
  color: #8C97A6;
  margin-top: 8rpx;
  display: block;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.order-date {
  font-size: 22rpx;
  color: #A8B4C5;
  margin-top: 8rpx;
  display: block;
}

.order-status {
  font-size: 26rpx;
  font-weight: 500;
  padding: 8rpx 20rpx;
  border-radius: 20rpx;
  flex-shrink: 0;
  margin-left: 20rpx;
}

.order-status.pending,
.order-status.waiting {
  color: #FA8C16;
  background: #FFF7E6;
}

.order-status.processing {
  color: #7B5CFA;
  background: #E6F4FF;
}

.order-status.completed,
.order-status.done {
  color: #52C41A;
  background: #F6FFED;
}

.order-status.refunded,
.order-status.cancelled {
  color: #8C97A6;
  background: #F8F7FC;
}

/* 后端状态（status 数字）配色 */
.order-status.status-1 { color: #FA8C16; background: #FFF7E6; }   /* 待支付 */
.order-status.status-2 { color: #7B5CFA; background: #E6F4FF; }   /* 已支付 */
.order-status.status-3,
.order-status.status-4 { color: #7B5CFA; background: #E6F4FF; }   /* 制作中/已发货 */
.order-status.status-5 { color: #52C41A; background: #F6FFED; }   /* 已完成 */
.order-status.status-6,
.order-status.status-7,
.order-status.status-8 { color: #8C97A6; background: #F8F7FC; }   /* 取消/退款中/已退款 */

/* ========== 缂佸矁娅ｆ慨鎼佸箑?========== */
.empty-order {
  padding-top: 188rpx;
  text-align: center;
  padding: 120rpx 0 60rpx;
}

.empty-icon {
  width: 160rpx;
  height: 160rpx;
  margin-bottom: 32rpx;
}

.empty-text {
  font-size: 30rpx;
  color: #8C97A6;
  font-weight: 500;
  display: block;
}

.empty-sub {
  font-size: 24rpx;
  color: #A8B4C5;
  margin-top: 12rpx;
  display: block;
}
