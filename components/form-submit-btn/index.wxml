<!--components/form-submit-btn/index.wxml-->
<view class="submit-bar">
  <button class="submit-btn {{enabled ? 'active' : 'disabled'}}" disabled="{{!enabled}}" bindtap="onSubmit">
    立即办理
  </button>
</view>
<view class="bottom-placeholder"></view>
