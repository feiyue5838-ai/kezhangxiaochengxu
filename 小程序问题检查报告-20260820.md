# 蓉城企服小程序全套问题检查报告

> 检查日期：2026-08-20 ｜ 检查范围：`rongcheng-miniprogram`（master 分支，工作区干净）
> 检查方式：ESLint 静态检查 + 页面/分包完整性校验 + 关键业务页面人工审查 + 废弃 API 扫描

---

## 检查结果总览

| 项目 | 结果 |
|---|---|
| ESLint | ✅ 干净（3 error 均在 dev 脚本 `fix_order_list.js`，非生产代码） |
| 页面完整性 | ✅ 62 个页面四件套齐全，tabBar 图标齐全 |
| 依赖引用 | ✅ 无失效 require（3 处命中均为注释误报/dev 脚本） |
| 业务逻辑 | ⚠️ 发现 **6 个严重问题、8 个中等问题、6 个轻微问题** |

---

## 🔴 严重问题（必须修复，影响线上功能）

### S1. 订单评价图片上传功能是坏的（真实 BUG）
- 位置：`utils/api.js:1158` `uploadReviewImage`
- 问题：内部调用了 `request()`，而 `request()` 封装的是 `wx.request()`（JSON 接口），**不支持** `filePath`/`name` 文件上传参数。真正应使用 `wx.uploadFile()`。
- 实际调用点：`pages/order/rate/index.js:92` —— 用户提交订单评价时上传图片必然失败。
- 修复：改用 `wx.uploadFile` 封装（参考同文件 `uploadFile` 实现）。

### S2. 个人中心没有退出登录入口
- 位置：`pages/profile/index.js` + `index.wxml`
- 问题：整个页面无 `退出/登出/logout` 任何实现。用户登录后**无法主动清除登录态**（包括网点 token）。
- 影响：换账号必须清小程序缓存；网点 token 残留存在越权风险。
- 修复：增加退出登录按钮，调用 `auth.logout()` 并 `refreshAuthState()`。

### S3. 供应商端页面无网点登录守卫
- 位置：`pages/supplier/index/index.js:37-43`、`pages/supplier/order-detail/index.js`
- 问题：`onLoad` 只读取 `outletInfo`，**不检查 `outletToken`**。未登录网点账号可直接进入工作台并发起请求。
- 连带问题：供应商/网点接口 401 时（`api.js` 301-305、342-345、511-515、749-752、918-924 行）只清 storage，**不跳转登录页**，与用户端 401 跳 `/pages/auth/index` 行为不一致。
- 修复：页面 `onShow`/`onLoad` 校验 `outletToken`，无则重定向 `/pages/outlet-binding/index`；401 统一跳登录页。

### S4. 售后申请撤销是假实现
- 位置：`pages/aftersale/detail/index.js:75-86` `cancelApply()`
- 问题：只弹 toast「已撤销」+ `navigateBack`，**未调用任何后端接口**。用户以为撤销了，实际售后申请仍然有效，后端仍会处理退款。
- 修复：调用后端撤销售后接口（需后端配合，如 `DELETE /api/after-sales/user/:id`），成功后刷新状态。

### S5. 售后申请页无防重复提交 + 无金额校验
- 位置：`pages/aftersale/apply/index.js` `submitApply`（79-95 行）
- 问题：
  1. 无 `submitting` 防重标志，连续点击可对同一订单**重复提交多个售后申请**；
  2. 无「订单是否已在售后/退款中」前置检查；
  3. 无退款金额字段和上限校验（部分退款场景无法限制金额）；
  4. 页面无登录守卫。
- 修复：加防重锁 + 订单售后状态前置检查 + 金额上限校验。

### S6. 订单详情页支付流程缺陷
- 位置：`pages/order/detail/index.js`
- 问题：
  1. **第 120 行** `api.v2Request ? null : null;` —— 完全无效的死代码；
  2. **开发模式支付分支**（112-129 行）：用户确认「模拟支付成功」后仅调用 `this.loadOrder()` 刷新页面，**没有调用后端 dev 确认接口**，支付状态不会真正变更；
  3. **第 111 行** `isSubmitting` 在获取支付参数后立即重置为 false，此时 `wx.requestPayment` 尚未调起，支付弹窗期间可重复点击；
  4. `onLoad` 无登录检查，401 时用户看到空白页无提示。
- 修复：删除死代码；开发模式调用 `devConfirmPay` 类接口；`isSubmitting` 在支付 success/fail 回调后再重置；`onLoad` 加 `auth.checkAuth(this)`。

---

## 🟠 中等问题（建议尽快修复）

### M1. 订单状态映射体系混乱（三处冲突）
- 现状：`utils/order-status.js`（V1 数字状态）+ `utils/order-status-v2.js`（V2 字符串状态）并存，且各页面自行硬编码：
  - **售后页**（`aftersale/list`、`aftersale/detail`）：`{7:'待处理', 8:'退款中', 9:'已完成'}`
  - **order-status.js**：`{7:'售后中', 9:'已退款'}` —— 同一数字状态文案冲突；
  - **供应商端**：`accepted:'已接单'`，**order-status-v2.js**：`accepted:'制作中'` —— 文案矛盾；
  - 供应商 `STATUS_TABS` 用 `pending`，后端履约状态为 `assigned`，映射缺失（列表可能显示原始英文字符串）。
- 建议：收敛为单一状态映射文件（单一事实源），页面统一引用。

### M2. `auth.isLogin()` 与 `globalData.isLogin` 逻辑不一致
- 位置：`utils/auth.js:9` 与 `refreshAuthState`（99 行）
- 问题：`isLogin()` 只查 `token`；而 `refreshAuthState` 中 `isLogin = token || isGuest`。游客模式时 Storage `isLogin=true` 但 `auth.isLogin()` 返回 `false`，依赖两处逻辑的页面行为可能矛盾。

### M3. `utils/auth.js:78` 注释引用不存在的函数
- 注释写着「网点账号独立登出请用 `outletLogout()`」，但项目全量搜索该函数**不存在、未导出**。误导维护者。

### M4. `utils/api.js` 重复定义 `outletRequest`
- 326 行独立函数 + 886 行模块导出，两份几乎相同的实现。维护时容易只改一处造成行为不一致。

### M5. 订单列表页残留测试模式代码（生产包会执行）
- 位置：`pages/order/list/index.js:91-95、107、172-181`
- 问题：URL 加 `?test=1` 即写入 `__test_mode__`，测试模式下注入 5 条假订单（`TEST_SEAL_001~004`、含假日期）。这些代码在生产包中仍会执行，存在误触风险。
- 建议：删除或按 `__DEV__` 编译开关隔离。

### M6. 订阅消息模板 ID 为空
- 位置：`utils/config.js` `WECHAT_SUBSCRIBE_TEMPLATE_ID = ''`
- 问题：门店订阅消息功能无法工作。需在小程序后台申请模板后填入。

### M7. `utils/api.js` 存在编码损坏字符
- 文件多处出现 `?` 乱码（如「直接返?data」「需要替换?HTTPS 公网域名」「?auth.request 行为对齐」），疑似历史编码损坏，建议全量清理。

### M8. 供应商订单详情拉取方式低效且静默吞错
- 位置：`pages/supplier/order-detail/index.js` `_fetchFromList`（46-85 行）
- 问题：遍历 4 个状态各拉 50 条拼详情，**订单超过 50 条会漏查**；所有请求 `.catch(()=>{})` 静默吞错，全部失败时无任何错误提示；`_doAction` 失败仅 `console.error`，用户无感知。

---

## 🟡 轻微问题 / 优化建议

### L1. 废弃 API `wx.getSystemInfoSync`（7 处）
- 涉及：`app.js`（有兜底）、`components/half-screen-popup`、`components/nav-bar`、`pages/newspaper/court`、`pages/newspaper/creditor`、`utils/api.js`、`utils/common.js`
- 建议：替换为 `wx.getWindowInfo()`（基础库 2.20.1+）。

### L2. `pages/seal/form/index.wxml` wx:for 缺 wx:key
- 4 个循环仅 1 个带 key，渲染列表有 diff 性能与状态复用风险。

### L3. `pages/seal/form/index.js` 硬编码 7 处 UUID
- 场景 ID、分类 ID、电子印章 subCategoryId 等均为硬编码，与后端数据耦合脆弱，建议提取配置或动态获取。

### L4. setData 性能优化点
- `pages/seal/order-confirm/index.js`：初始 data 硬编码 47 项 `singleSeals` + 7 项 `packages`；`loadSavedData` 连续 5 次独立 setData；输入框每次按键 setData + 2 次 setStorageSync。
- `pages/order/list/index.js`：三连 setData（`allList` → `filterList` → `loading`）。
- 建议：合并批量 setData，常量提出模块级。

### L5. `utils/auth.js logout()` 重复删除 `openid`
- 60 行与 76 行两次 `removeStorageSync('openid')`，冗余。

### L6. `services/seal.js` `cancelOrder` 未真正调用 API
- 仅 toast「请联系客服取消订单」，但 `utils/api.js` 已有 `cancelSealOrder` 接口可用，属于服务层与 API 层断连。

---

## 未发现问题的方面 ✅

- **页面/分包完整性**：62 个页面、16 个分包，wxml/wxss/js/json 四件套全部齐全。
- **tabBar 图标**：10 个图标文件全部存在。
- **重复下单防护**：`order-confirm` 页 `isSubmitting` + `_createdOrderId` 复用机制完善。
- **支付参数完整性**：真实支付分支 `wx.requestPayment` 参数齐全，cancel 检测正确，成功后轮询后端。
- **登录页**：防重、失败处理、游客模式处理合理。
- **代码规范**：ESLint 全量通过（仅 dev 脚本 3 处 error）。

---

## 修复优先级建议

1. **立即修复（上线阻断）**：S1 评价图片上传、S2 退出登录、S4 售后撤销假实现
2. **尽快修复（体验/安全）**：S3 供应商端登录守卫、S5 售后防重、S6 支付流程死代码
3. **下次迭代**：M1 状态映射统一、M5 测试代码清理、L1 废弃 API 替换
